[package]
name = "rev-replay"
version = "0.1.0"
edition = "2021"

[dependencies]
rev-core = { path = "../core" }
rev-delta = { path = "../delta" }
serde = { workspace = true }
serde_json = { workspace = true }
